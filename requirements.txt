# --- Backend / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- LLM (Gemini, chat + function calling) ---
google-genai>=1.0

# --- Embeddings locales multilingües (default; ver decisions.md) ---
sentence-transformers>=3.0

# --- Vector store ---
# 1.x trae binarios precompilados (en Windows/py3.12 la rama 0.5.x exige compilar chroma-hnswlib).
chromadb>=1.0,<2.0

# --- HTTP (API de cartas) + reintentos ---
httpx==0.28.1
tenacity==9.0.0

# --- Parsing del reglamento (PDF) ---
pdfplumber==0.11.4

# --- Tests / calidad ---
pytest==8.3.4
pytest-asyncio==0.25.0
respx==0.22.0
